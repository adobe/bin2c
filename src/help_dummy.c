#include <stdlib.h>

/// We use bin2c to include help.txt, so we generate a bootstrapping
/// binary without the actual help.txt and this file in it's stead.

const char blob_help[] = "";
const size_t blob_help_len = 0;
